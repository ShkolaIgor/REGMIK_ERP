# Фільтрація накладних в 1С - оптимізація передачі даних

## Мета
Перенести фільтрацію з ERP системи в 1С для оптимізації продуктивності:
- **Вхідні накладні**: тільки товарні позиції (без послуг/витрат)
- **Вихідні рахунки**: товари + послуги (повна функціональність для клієнтів)

## Переваги
- Зменшення навантаження на мережу
- Швидша обробка в ERP
- Менше "порожніх" документів для аналізу

## Код для вхідних накладних (оновлений)

```1c
Функция ПолучитьТоварныеНакладные()
    Попытка
        ЗаписатьВЛогФайл("🔍 Початок пошуку тільки товарних накладних...");
        
        Запрос = Новый Запрос;
        Запрос.Текст = "
        |ВЫБРАТЬ
        |    Док.Ссылка КАК СсылкаДокумента,
        |    Док.Номер КАК НомерДокумента,
        |    Док.Дата КАК ДатаДокумента,
        |    ВЫБОР
        |        КОГДА Контрагенты.Наименование <> """"
        |        ТОГДА Контрагенты.Наименование
        |        ИНАЧЕ ""Постачальник не вказано""
        |    КОНЕЦ КАК Постачальник,
        |    Док.СуммаДокумента КАК СуммаДокумента,
        |    Валюты.Код КАК КодВалюты
        |ИЗ
        |    Документ.ПоступлениеТоваровИУслуг КАК Док
        |    ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Контрагенты КАК Контрагенты
        |        ПО Док.Контрагент = Контрагенты.Ссылка
        |    ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Валюты КАК Валюты
        |        ПО Док.ВалютаДокумента = Валюты.Ссылка
        |ГДЕ
        |    Док.Дата >= &ДатаНачала
        |    И Док.Проведен = ИСТИНА
        |    И ЕСТЬNULL(Док.ПометкаУдаления, ЛОЖЬ) = ЛОЖЬ
        |    // ФІЛЬТР: тільки документи з товарними позиціями
        |    И Док.Ссылка В (
        |        ВЫБРАТЬ РАЗЛИЧНЫЕ
        |            ТабличнаяЧасть.Ссылка
        |        ИЗ
        |            Документ.ПоступлениеТоваровИУслуг.Товары КАК ТабличнаяЧасть
        |        ГДЕ
        |            НЕ (ВРег(ТабличнаяЧасть.Номенклатура.Наименование) ПОДОБНО "%ПОСЛУГ%"
        |                ИЛИ ВРег(ТабличнаяЧасть.Номенклатура.Наименование) ПОДОБНО "%УСЛУГ%"
        |                ИЛИ ВРег(ТабличнаяЧасть.Номенклатура.Наименование) ПОДОБНО "%РОБОТ%"
        |                ИЛИ ВРег(ТабличнаяЧасть.Номенклатура.Наименование) ПОДОБНО "%РАБОТ%")
        |    )
        |УПОРЯДОЧИТЬ ПО
        |    Док.Дата УБЫВ
        |";
        
        // Останні 3 місяці для оптимізації
        ДатаНачала = ТекущаяДата() - 90*24*3600;
        Запрос.УстановитьПараметр("ДатаНачала", ДатаНачала);
        
        Результат = Запрос.Выполнить();
        Выборка = Результат.Выбрать();
        
        МассивНакладных = Новый Массив;
        
        Пока Выборка.Следующий() Цикл
            // Отримуємо тільки товарні позиції
            ТоварныеПозиции = ПолучитьТоварныеПозицииНакладной(Выборка.СсылкаДокумента);
            
            // Пропускаємо якщо немає товарних позицій
            Если ТоварныеПозиции.Количество() = 0 Тогда
                Продолжить;
            КонецЕсли;
            
            Накладная = Новый Структура;
            Накладная.Вставить("НомерДокумента", Выборка.НомерДокумента);
            Накладная.Вставить("ДатаДокумента", Формат(Выборка.ДатаДокумента, "ДФ=yyyy-MM-dd"));
            Накладная.Вставить("Постачальник", Выборка.Постачальник);
            Накладная.Вставить("СуммаДокумента", Выборка.СуммаДокумента);
            Накладная.Вставить("КодВалюты", Выборка.КодВалюты);
            Накладная.Вставить("Позиції", ТоварныеПозиции);
            Накладная.Вставить("КількістьТоварів", ТоварныеПозиции.Количество());
            
            МассивНакладных.Добавить(Накладная);
        КонецЦикла;
        
        ЗаписатьВЛогФайл("✅ Знайдено " + МассивНакладных.Количество() + " товарних накладних");
        Возврат МассивНакладных;
        
    Исключение
        ТекстОшибки = ОписаниеОшибки();
        ЗаписатьВЛогФайл("❌ Помилка отримання товарних накладних: " + ТекстОшибки);
        Возврат Новый Массив;
    КонецПопытки;
КонецФункции

Функция ПолучитьТоварныеПозицииНакладной(СсылкаДокумента)
    Попытка
        Запрос = Новый Запрос;
        Запрос.Текст = "
        |ВЫБРАТЬ
        |    ТабличнаяЧасть.Номенклатура.Наименование КАК НаименованиеТовара,
        |    ТабличнаяЧасть.Количество КАК Количество,
        |    ТабличнаяЧасть.Цена КАК Цена,
        |    ТабличнаяЧасть.Сумма КАК Сумма,
        |    ТабличнаяЧасть.Номенклатура.Код КАК КодТовара
        |ИЗ
        |    Документ.ПоступлениеТоваровИУслуг.Товары КАК ТабличнаяЧасть
        |ГДЕ
        |    ТабличнаяЧасть.Ссылка = &СсылкаДокумента
        |    // ТІЛЬКИ ТОВАРИ (виключаємо послуги за назвою)
        |    И НЕ (ВРег(ТабличнаяЧасть.Номенклатура.Наименование) ПОДОБНО "%ПОСЛУГ%"
        |        ИЛИ ВРег(ТабличнаяЧасть.Номенклатура.Наименование) ПОДОБНО "%УСЛУГ%"
        |        ИЛИ ВРег(ТабличнаяЧасть.Номенклатура.Наименование) ПОДОБНО "%РОБОТ%"
        |        ИЛИ ВРег(ТабличнаяЧасть.Номенклатура.Наименование) ПОДОБНО "%РАБОТ%")
        |УПОРЯДОЧИТЬ ПО
        |    ТабличнаяЧасть.НомерСтроки
        |";
        
        Запрос.УстановитьПараметр("СсылкаДокумента", СсылкаДокумента);
        Результат = Запрос.Выполнить();
        Выборка = Результат.Выбрать();
        
        МассивПозиций = Новый Массив;
        
        Пока Выборка.Следующий() Цикл
            Позиция = Новый Структура;
            Позиция.Вставить("НаименованиеТовара", Выборка.НаименованиеТовара);
            Позиция.Вставить("Количество", Выборка.Количество);
            Позиция.Вставить("Цена", Выборка.Цена);
            Позиция.Вставить("Сумма", Выборка.Сумма);
            Позиция.Вставить("КодТовара", Выборка.КодТовара);
            
            МассивПозиций.Добавить(Позиция);
        КонецЦикла;
        
        Возврат МассивПозиций;
        
    Исключение
        ЗаписатьВЛогФайл("❌ Помилка отримання позицій накладної: " + ОписаниеОшибки());
        Возврат Новый Массив;
    КонецПопытки;
КонецФункции
```

## Код для вихідних рахунків (товари + послуги)

```1c
Функция ПолучитьВсіВихідніРахунки()
    Попытка
        ЗаписатьВЛогФайл("🔍 Початок пошуку тільки товарних вихідних рахунків...");
        
        Запрос = Новый Запрос;
        Запрос.Текст = "
        |ВЫБРАТЬ
        |    Док.Ссылка КАК СсылкаДокумента,
        |    Док.Номер КАК НомерДокумента,
        |    Док.Дата КАК ДатаДокумента,
        |    ВЫБОР
        |        КОГДА Контрагенты.Наименование <> """"
        |        ТОГДА Контрагенты.Наименование
        |        ИНАЧЕ ""Клієнт не вказано""
        |    КОНЕЦ КАК Клієнт,
        |    Док.СуммаДокумента КАК СуммаДокумента,
        |    Валюты.Код КАК КодВалюты,
        |    ВЫБОР
        |        КОГДА Док.Проведен = ИСТИНА ТОГДА ""paid""
        |        ИНАЧЕ ""unpaid""
        |    КОНЕЦ КАК СтатусОплати
        |ИЗ
        |    Документ.СчетНаОплатуПокупателю КАК Док
        |    ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Контрагенты КАК Контрагенты
        |        ПО Док.Контрагент = Контрагенты.Ссылка
        |    ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Валюты КАК Валюты
        |        ПО Док.ВалютаДокумента = Валюты.Ссылка
        |ГДЕ
        |    Док.Дата >= &ДатаНачала
        |    И ЕСТЬNULL(Док.ПометкаУдаления, ЛОЖЬ) = ЛОЖЬ
        |    // БЕЗ ФІЛЬТРАЦІЇ - передаємо всі рахунки (товари + послуги)
        |УПОРЯДОЧИТЬ ПО
        |    Док.Дата УБЫВ
        |";
        
        // Останні 3 місяці для оптимізації
        ДатаНачала = ТекущаяДата() - 90*24*3600;
        Запрос.УстановитьПараметр("ДатаНачала", ДатаНачала);
        
        Результат = Запрос.Выполнить();
        Выборка = Результат.Выбрать();
        
        МассивРахунків = Новый Массив;
        
        Пока Выборка.Следующий() Цикл
            // Отримуємо всі позиції рахунку (товари + послуги)
            ВсіПозиции = ПолучитьВсіПозицииРахунку(Выборка.СсылкаДокумента);
            
            Рахунок = Новый Структура;
            Рахунок.Вставить("НомерДокумента", Выборка.НомерДокумента);
            Рахунок.Вставить("ДатаДокумента", Формат(Выборка.ДатаДокумента, "ДФ=yyyy-MM-dd"));
            Рахунок.Вставить("Клієнт", Выборка.Клієнт);
            Рахунок.Вставить("СуммаДокумента", Выборка.СуммаДокумента);
            Рахунок.Вставить("КодВалюты", Выборка.КодВалюты);
            Рахунок.Вставить("СтатусОплати", Выборка.СтатусОплати);
            Рахунок.Вставить("Позиції", ВсіПозиции);
            Рахунок.Вставить("КількістьПозицій", ВсіПозиции.Количество());
            
            МассивРахунків.Добавить(Рахунок);
        КонецЦикла;
        
        ЗаписатьВЛогФайл("✅ Знайдено " + МассивРахунків.Количество() + " товарних рахунків");
        Возврат МассивРахунків;
        
    Исключение
        ТекстОшибки = ОписаниеОшибки();
        ЗаписатьВЛогФайл("❌ Помилка отримання товарних рахунків: " + ТекстОшибки);
        Возврат Новый Массив;
    КонецПопытки;
КонецФункции

Функция ПолучитьВсіПозицииРахунку(СсылкаДокумента)
    Попытка
        Запрос = Новый Запрос;
        Запрос.Текст = "
        |ВЫБРАТЬ
        |    ТабличнаяЧасть.Номенклатура.Наименование КАК НаименованиеПозиции,
        |    ТабличнаяЧасть.Количество КАК Количество,
        |    ТабличнаяЧасть.Цена КАК Цена,
        |    ТабличнаяЧасть.Сумма КАК Сумма,
        |    ТабличнаяЧасть.Номенклатура.Код КАК КодПозиции,
        |    ТабличнаяЧасть.Номенклатура.ТипНоменклатуры КАК ТипПозиції
        |ИЗ
        |    Документ.СчетНаОплатуПокупателю.Товары КАК ТабличнаяЧасть
        |ГДЕ
        |    ТабличнаяЧасть.Ссылка = &СсылкаДокумента
        |УПОРЯДОЧИТЬ ПО
        |    ТабличнаяЧасть.НомерСтроки
        |";
        
        Запрос.УстановитьПараметр("СсылкаДокумента", СсылкаДокумента);
        Результат = Запрос.Выполнить();
        Выборка = Результат.Выбрать();
        
        МассивПозиций = Новый Массив;
        
        Пока Выборка.Следующий() Цикл
            Позиция = Новый Структура;
            Позиция.Вставить("НаименованиеПозиції", Выборка.НаименованиеПозиції);
            Позиция.Вставить("Количество", Выборка.Количество);
            Позиция.Вставить("Цена", Выборка.Цена);
            Позиция.Вставить("Сумма", Выборка.Сумма);
            Позиция.Вставить("КодПозиції", Выборка.КодПозиції);
            Позиция.Вставить("ТипПозиції", Строка(Выборка.ТипПозиції)); // Товар, Послуга, Робота
            
            МассивПозиций.Добавить(Позиция);
        КонецЦикла;
        
        Возврат МассивПозиций;
        
    Исключение
        ЗаписатьВЛогФайл("❌ Помилка отримання позицій рахунку: " + ОписаниеОшибки());
        Возврат Новый Массив;
    КонецПопытки;
КонецФункции
```

## Оновлений основний HTTP-обробник

```1c
Функция ОбработкаВызоваHTTPСервиса(Запрос)
    Попытка
        // Отримуємо параметри
        ПараметрыЗапроса = ПолучитьПараметрыИзТелаЗапроса(Запрос.ПолучитьТелоКакСтроку());
        
        Действие = ПараметрыЗапроса.Получить("action");
        Лимит = Число(ПараметрыЗапроса.Получить("limit"));
        
        Если Лимит = 0 Тогда
            Лимит = 50; // За замовчуванням
        КонецЕсли;
        
        Если Действие = "getInvoices" Тогда
            // ТІЛЬКИ ТОВАРНІ НАКЛАДНІ
            МассивДанных = ПолучитьТоварныеНакладные();
            
        ИначеЕсли Действие = "getOutgoingInvoices" Тогда
            // ВСІ РАХУНКИ (товари + послуги)
            МассивДанных = ПолучитьВсіВихідніРахунки();
            
        Иначе
            ЗаписатьВЛогФайл("❌ Невідома дія: " + Действие);
            Возврат СоздатьОтветHTTP(400, "Невідома дія");
        КонецЕсли;
        
        // Обмежуємо кількість результатів
        Если МассивДанных.Количество() > Лимит Тогда
            МассивВыборки = Новый Массив;
            Для Индекс = 0 По Лимит - 1 Цикл
                МассивВыборки.Добавить(МассивДанных[Индекс]);
            КонецЦикла;
            МассивДанных = МассивВыборки;
        КонецЕсли;
        
        JSONОтвет = ПреобразоватьВJSON(МассивДанных);
        
        ЗаписатьВЛогФайл("✅ Відправлено " + МассивДанных.Количество() + " товарних документів для дії: " + Действие);
        
        Возврат СоздатьОтветHTTP(200, JSONОтвет);
        
    Исключение
        ТекстОшибки = ОписаниеОшибки();
        ЗаписатьВЛогФайл("❌ Критична помилка HTTP-обробника: " + ТекстОшибки);
        Возврат СоздатьОтветHTTP(500, "Помилка сервера: " + ТекстОшибки);
    КонецПопытки;
КонецФункции
```

## Переваги оптимізованого підходу

1. **Продуктивність**: Фільтрація на рівні БД 1С швидша ніж в ERP
2. **Мережа**: Менше даних передається по мережі (для вхідних накладних)
3. **Логіка**: 
   - **Вхідні накладні**: тільки товари (оптимізація складського обліку)
   - **Вихідні рахунки**: товари + послуги (повний функціонал для клієнтів)
4. **Масштабованість**: При збільшенні кількості документів навантаження не зростає критично
5. **Точність**: Використання ТипНоменклатуры для точної категоризації

## Різниця підходів

### Вхідні накладні (від постачальників)
- Фільтруються на рівні 1С - тільки товарні позиції
- Послуги та витрати виключаються для оптимізації складського обліку

### Вихідні рахунки (клієнтам)
- Передаються всі позиції - товари, послуги, роботи
- Клієнтам можна виставляти рахунки за будь-які види номенклатури

## Реалізація
Замініть існуючі функції в HTTP-сервісі 1С цим кодом для оптимізації роботи системи.