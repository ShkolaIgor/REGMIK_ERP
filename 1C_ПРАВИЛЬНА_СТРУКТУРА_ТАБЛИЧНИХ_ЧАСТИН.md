# ПРАВИЛЬНА СТРУКТУРА ТАБЛИЧНИХ ЧАСТИН 1С

## Виявлена структура документу ПоступлениеТоваровИУслуг

```
Документ.ПоступлениеТоваровИУслуг
├── Товары (табличная часть)
│   ├── Номенклатура
│   ├── Количество  
│   ├── Сумма
│   └── інші поля...
└── Услуги (табличная часть)
    ├── Номенклатура
    ├── Количество
    ├── Сумма
    └── інші поля...
```

## ВИПРАВЛЕНИЙ КОД для фільтрації тільки товарів

### Функція отримання тільки товарних накладних

```1c
Функция ПолучитьТоварныеНакладныеПравильно()
    Попытка
        ЗаписатьВЛогФайл("🔍 Пошук накладних ТІЛЬКИ з товарними позиціями...");
        
        Запрос = Новый Запрос;
        Запрос.Текст = "
        |ВЫБРАТЬ
        |    Док.Ссылка КАК СсылкаДокумента,
        |    Док.Номер КАК НомерДокумента,
        |    Док.Дата КАК ДатаДокумента,
        |    ВЫБОР
        |        КОГДА Контрагенты.Наименование <> """"
        |        ТОГДА Контрагенты.Наименование
        |        ИНАЧЕ ""Постачальник не вказано""
        |    КОНЕЦ КАК Постачальник,
        |    Док.СуммаДокумента КАК СуммаДокумента,
        |    Валюты.Код КАК КодВалюты
        |ИЗ
        |    Документ.ПоступлениеТоваровИУслуг КАК Док
        |    ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Контрагенты КАК Контрагенты
        |        ПО Док.Контрагент = Контрагенты.Ссылка
        |    ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Валюты КАК Валюты
        |        ПО Док.ВалютаДокумента = Валюты.Ссылка
        |ГДЕ
        |    Док.Дата >= &ДатаНачала
        |    И Док.Проведен = ИСТИНА
        |    И ЕСТЬNULL(Док.ПометкаУдаления, ЛОЖЬ) = ЛОЖЬ
        |    // ФІЛЬТР: тільки документи які мають товарні позиції (табличка Товары)
        |    И Док.Ссылка В (
        |        ВЫБРАТЬ РАЗЛИЧНЫЕ
        |            Товары.Ссылка
        |        ИЗ
        |            Документ.ПоступлениеТоваровИУслуг.Товары КАК Товары
        |        ГДЕ
        |            Товары.Количество > 0
        |    )
        |УПОРЯДОЧИТЬ ПО
        |    Док.Дата УБЫВ
        |";
        
        // Останні 3 місяці
        ДатаНачала = ТекущаяДата() - 90*24*3600;
        Запрос.УстановитьПараметр("ДатаНачала", ДатаНачала);
        
        Результат = Запрос.Выполнить();
        Выборка = Результат.Выбрать();
        
        МассивНакладных = Новый Массив;
        
        Пока Выборка.Следующий() Цикл
            // Отримуємо тільки товарні позиції з табличної частини Товары
            ТоварныеПозиции = ПолучитьТоварныеПозицииПравильно(Выборка.СсылкаДокумента);
            
            // Пропускаємо якщо немає товарних позицій
            Если ТоварныеПозиции.Количество() = 0 Тогда
                Продолжить;
            КонецЕсли;
            
            Накладная = Новый Структура;
            Накладная.Вставить("НомерДокумента", Выборка.НомерДокумента);
            Накладная.Вставить("ДатаДокумента", Формат(Выборка.ДатаДокумента, "ДФ=yyyy-MM-dd"));
            Накладная.Вставить("Постачальник", Выборка.Постачальник);
            Накладная.Вставить("СуммаДокумента", Выборка.СуммаДокумента);
            Накладная.Вставить("КодВалюты", Выборка.КодВалюты);
            Накладная.Вставить("Позиції", ТоварныеПозиции);
            Накладная.Вставить("КількістьТоварів", ТоварныеПозиции.Количество());
            
            МассивНакладных.Добавить(Накладная);
        КонецЦикла;
        
        ЗаписатьВЛогФайл("✅ Знайдено " + МассивНакладных.Количество() + " накладних з товарами");
        Возврат МассивНакладных;
        
    Исключение
        ТекстОшибки = ОписаниеОшибки();
        ЗаписатьВЛогФайл("❌ Помилка: " + ТекстОшибки);
        Возврат Новый Массив;
    КонецПопытки;
КонецФункции
```

### Функція отримання товарних позицій з табличної частини Товары

```1c
Функция ПолучитьТоварныеПозицииПравильно(СсылкаДокумента)
    Попытка
        Запрос = Новый Запрос;
        Запрос.Текст = "
        |ВЫБРАТЬ
        |    Товары.Номенклатура.Наименование КАК НаименованиеТовара,
        |    Товары.Номенклатура.Код КАК КодТовара,
        |    Товары.Количество КАК Количество,
        |    Товары.Цена КАК Цена,
        |    Товары.Сумма КАК Сумма,
        |    Товары.НомерСтроки КАК НомерСтроки
        |ИЗ
        |    Документ.ПоступлениеТоваровИУслуг.Товары КАК Товары
        |ГДЕ
        |    Товары.Ссылка = &СсылкаДокумента
        |    И Товары.Количество > 0
        |УПОРЯДОЧИТЬ ПО
        |    Товары.НомерСтроки
        |";
        
        Запрос.УстановитьПараметр("СсылкаДокумента", СсылкаДокумента);
        Результат = Запрос.Выполнить();
        Выборка = Результат.Выбрать();
        
        МассивПозиций = Новый Массив;
        
        Пока Выборка.Следующий() Цикл
            Позиция = Новый Структура;
            Позиция.Вставить("НаименованиеТовара", Выборка.НаименованиеТовара);
            Позиция.Вставить("КодТовара", Выборка.КодТовара);
            Позиция.Вставить("Количество", Выборка.Количество);
            Позиция.Вставить("Цена", Выборка.Цена);
            Позиция.Вставить("Сумма", Выборка.Сумма);
            Позиция.Вставить("НомерСтроки", Выборка.НомерСтроки);
            
            МассивПозиций.Добавить(Позиция);
        КонецЦикла;
        
        ЗаписатьВЛогФайл("📦 Знайдено " + МассивПозиций.Количество() + " товарних позицій");
        Возврат МассивПозиций;
        
    Исключение
        ЗаписатьВЛогФайл("❌ Помилка отримання товарних позицій: " + ОписаниеОшибки());
        Возврат Новый Массив;
    КонецПопытки;
КонецФункции
```

### Функція отримання послуг (для діагностики)

```1c
Функция ПолучитьПослугиДокумента(СсылкаДокумента)
    Попытка
        Запрос = Новый Запрос;
        Запрос.Текст = "
        |ВЫБРАТЬ
        |    Услуги.Номенклатура.Наименование КАК НаименованиеУслуги,
        |    Услуги.Номенклатура.Код КАК КодУслуги,
        |    Услуги.Количество КАК Количество,
        |    Услуги.Цена КАК Цена,
        |    Услуги.Сумма КАК Сумма
        |ИЗ
        |    Документ.ПоступлениеТоваровИУслуг.Услуги КАК Услуги
        |ГДЕ
        |    Услуги.Ссылка = &СсылкаДокумента
        |    И Услуги.Количество > 0
        |УПОРЯДОЧИТЬ ПО
        |    Услуги.НомерСтроки
        |";
        
        Запрос.УстановитьПараметр("СсылкаДокумента", СсылкаДокумента);
        Результат = Запрос.Выполнить();
        Выборка = Результат.Выбрать();
        
        МассивПослуг = Новый Массив;
        
        Пока Выборка.Следующий() Цикл
            Услуга = Новый Структура;
            Услуга.Вставить("НаименованиеУслуги", Выборка.НаименованиеУслуги);
            Услуга.Вставить("КодУслуги", Выборка.КодУслуги);
            Услуга.Вставить("Количество", Выборка.Количество);
            Услуга.Вставить("Цена", Выборка.Цена);
            Услуга.Вставить("Сумма", Выборка.Сумма);
            
            МассивПослуг.Добавить(Услуга);
        КонецЦикла;
        
        ЗаписатьВЛогФайл("🛠️ Знайдено " + МассивПослуг.Количество() + " послуг (виключаються з товарних накладних)");
        Возврат МассивПослуг;
        
    Исключение
        ЗаписатьВЛогФайл("❌ Помилка отримання послуг: " + ОписаниеОшибки());
        Возврат Новый Массив;
    КонецПопытки;
КонецФункции
```

## ОНОВЛЕНИЙ основний HTTP-обробник

```1c
Функция ОбработкаВызоваHTTPСервиса(Запрос)
    Попытка
        ПараметрыЗапроса = ПолучитьПараметрыИзТелаЗапроса(Запрос.ПолучитьТелоКакСтроку());
        
        Действие = ПараметрыЗапроса.Получить("action");
        Лимит = Число(ПараметрыЗапроса.Получить("limit"));
        
        Если Лимит = 0 Тогда
            Лимит = 50;
        КонецЕсли;
        
        ЗаписатьВЛогФайл("📋 Запит: " + Действие + ", ліміт: " + Лимит);
        
        Если Действие = "getInvoices" Тогда
            // ТІЛЬКИ ТОВАРНІ НАКЛАДНІ (табличка Товары)
            МассивДанных = ПолучитьТоварныеНакладныеПравильно();
            
        ИначеЕсли Действие = "getOutgoingInvoices" Тогда
            // ВСІ РАХУНКИ (товари + послуги)
            МассивДанных = ПолучитьВсіВихідніРахунки();
            
        Иначе
            ЗаписатьВЛогФайл("❌ Невідома дія: " + Действие);
            МассивДанных = Новый Массив;
        КонецЕсли;
        
        // Застосовуємо ліміт
        Если МассивДанных.Количество() > Лимит Тогда
            Результат = Новый Массив;
            Для Индекс = 0 По Лимит - 1 Цикл
                Результат.Добавить(МассивДанных[Индекс]);
            КонецЦикла;
            МассивДанных = Результат;
        КонецЕсли;
        
        ЗаписатьВЛогФайл("✅ Повертаємо " + МассивДанных.Количество() + " документів");
        JSONСтрока = ПреобразоватьВJSON(МассивДанных);
        
        Ответ = Новый HTTPСервисОтвет(200);
        Ответ.УстановитьТелоИзСтроки(JSONСтрока, КодировкаТекста.UTF8);
        Ответ.Заголовки.Вставить("Content-Type", "application/json; charset=utf-8");
        
        Возврат Ответ;
        
    Исключение
        ТекстОшибки = ОписаниеОшибки();
        ЗаписатьВЛогФайл("❌ КРИТИЧНА ПОМИЛКА: " + ТекстОшибки);
        
        Ответ = Новый HTTPСервисОтвет(500);
        Ответ.УстановитьТелоИзСтроки("{""error"": ""Внутрішня помилка сервера""}", КодировкаТекста.UTF8);
        
        Возврат Ответ;
    КонецПопытки;
КонецФункции
```

## Переваги правильного підходу

1. **Точна фільтрація**: Використання табличної частини `Товары` замість `Услуги`
2. **Простота**: Не потрібні складні фільтри за назвами номенклатури
3. **Продуктивність**: Прямий доступ до потрібної табличної частини
4. **Надійність**: Виключає помилки з перерахуваннями та назвами

## Інструкція впровадження

1. Замініть функції в HTTP-сервісі 1С на правильні версії
2. Протестуйте запит `getInvoices` - тепер буде тільки товарні позиції
3. Перевірте лог для діагностики роботи
4. Для вихідних рахунків залишається функція без фільтрації