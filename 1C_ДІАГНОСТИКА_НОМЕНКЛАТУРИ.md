# Діагностика структури номенклатури в 1С

## Проблема
Помилка: `Неправильні параметри "Перечисление.ТипыНоменклатуры.Товар"`
Це означає, що в вашій 1С системі структура номенклатури відрізняється від стандартної.

## Діагностичний код для виявлення структури

```1c
Функция ДіагностикаНоменклатуры()
    Попытка
        ЗаписатьВЛогФайл("🔍 Початок діагностики структури номенклатури...");
        
        // 1. Перевіряємо поля в довіднику Номенклатура
        Запрос = Новый Запрос;
        Запрос.Текст = "
        |ВЫБРАТЬ ПЕРВЫЕ 10
        |    Номенклатура.Ссылка,
        |    Номенклатура.Наименование,
        |    Номенклатура.Код
        |ИЗ
        |    Справочник.Номенклатура КАК Номенклатура
        |";
        
        Результат = Запрос.Выполнить();
        ТаблицаРезультатов = Результат.Выгрузить();
        
        ЗаписатьВЛогФайл("📊 Знайдено номенклатури: " + ТаблицаРезультатов.Количество());
        
        // 2. Перевіряємо наявність поля ТипНоменклатуры
        ПерваяНоменклатура = ТаблицаРезультатов[0].Ссылка;
        
        Попытка
            ТипНоменклатуры = ПерваяНоменклатура.ТипНоменклатуры;
            ЗаписатьВЛогФайл("✅ Поле ТипНоменклатуры існує: " + Строка(ТипНоменклатуры));
        Исключение
            ЗаписатьВЛогФайл("❌ Поле ТипНоменклатуры НЕ існує");
        КонецПопытки;
        
        // 3. Перевіряємо наявність поля ВидНоменклатуры
        Попытка
            ВидНоменклатуры = ПерваяНоменклатура.ВидНоменклатуры;
            ЗаписатьВЛогФайл("✅ Поле ВидНоменклатуры існує: " + Строка(ВидНоменклатуры));
        Исключение
            ЗаписатьВЛогФайл("❌ Поле ВидНоменклатуры НЕ існує");
        КонецПопытки;
        
        // 4. Перевіряємо наявність поля ТипУслуги
        Попытка
            ТипУслуги = ПерваяНоменклатура.ТипУслуги;
            ЗаписатьВЛогФайл("✅ Поле ТипУслуги існує: " + Строка(ТипУслуги));
        Исключение
            ЗаписатьВЛогФайл("❌ Поле ТипУслуги НЕ існує");
        КонецПопытки;
        
        // 5. Перевіряємо метадані довідника
        МетаданныеНоменклатуры = Метаданные.Справочники.Номенклатура;
        ЗаписатьВЛогФайл("📝 Кількість реквізитів номенклатури: " + МетаданныеНоменклатуры.Реквизиты.Количество());
        
        Для Каждого Реквизит Из МетаданныеНоменклатуры.Реквизиты Цикл
            Если СтрНайти(ВРег(Реквизит.Имя), "ТИП") > 0 Тогда
                ЗаписатьВЛогФайл("🔹 Знайдено реквізит типу: " + Реквизит.Имя + " (" + Строка(Реквизит.Тип) + ")");
            КонецЕсли;
        КонецЦикла;
        
        // 6. Перевіряємо перерахування
        Попытка
            МетаданныеТипов = Метаданные.Перечисления;
            Для Каждого Перечисление Из МетаданныеТипов Цикл
                Если СтрНайти(ВРег(Перечисление.Имя), "ТИП") > 0 
                    ИЛИ СтрНайти(ВРег(Перечисление.Имя), "НОМЕНКЛАТУР") > 0 
                    ИЛИ СтрНайти(ВРег(Перечисление.Имя), "УСЛУГ") > 0 Тогда
                    ЗаписатьВЛогФайл("🔸 Знайдено перерахування: " + Перечисление.Имя);
                    
                    // Виводимо значення перерахування
                    Для Каждого ЗначениеПеречисления Из Перечисление.ЗначенияПеречисления Цикл
                        ЗаписатьВЛогФайл("   → " + ЗначениеПеречисления.Имя);
                    КонецЦикла;
                КонецЕсли;
            КонецЦикла;
        Исключение
            ЗаписатьВЛогФайл("❌ Помилка перевірки перерахувань: " + ОписаниеОшибки());
        КонецПопытки;
        
        ЗаписатьВЛогФайл("✅ Діагностика завершена");
        Возврат "OK";
        
    Исключение
        ТекстОшибки = ОписаниеОшибки();
        ЗаписатьВЛогФайл("❌ КРИТИЧНА ПОМИЛКА діагностики: " + ТекстОшибки);
        Возврат ТекстОшибки;
    КонецПопытки;
КонецФункции
```

## Альтернативні способи фільтрації

### Варіант 1: За назвою номенклатури
```1c
// Виключаємо послуги за ключовими словами в назві
И НЕ (ВРег(ТабличнаяЧасть.Номенклатура.Наименование) ПОДОБНО "%ПОСЛУГ%"
    ИЛИ ВРег(ТабличнаяЧасть.Номенклатура.Наименование) ПОДОБНО "%УСЛУГ%"
    ИЛИ ВРег(ТабличнаяЧасть.Номенклатура.Наименование) ПОДОБНО "%РОБОТ%"
    ИЛИ ВРег(ТабличнаяЧасть.Номенклатура.Наименование) ПОДОБНО "%РАБОТ%")
```

### Варіант 2: За групою номенклатури
```1c
// Виключаємо групи послуг
И НЕ ТабличнаяЧасть.Номенклатура.Родитель В (
    ВЫБРАТЬ Ссылка ИЗ Справочник.Номенклатура 
    ГДЕ ВРег(Наименование) ПОДОБНО "%УСЛУГ%" И ЭтоГруппа = ИСТИНА
)
```

### Варіант 3: За кодом номенклатури
```1c
// Якщо товари мають певні коди
И (ТабличнаяЧасть.Номенклатура.Код ПОДОБНО "ТОВ%"
    ИЛИ ТабличнаяЧасть.Номенклатура.Код ПОДОБНО "MAT%"
    ИЛИ ДЛИНА(ТабличнаяЧасть.Номенклатура.Код) >= 5)
```

## Рекомендації
1. Запустіть діагностичну функцію для виявлення структури
2. Оберіть найкращий варіант фільтрації для вашої системи
3. Замініть проблемний код на робочий варіант